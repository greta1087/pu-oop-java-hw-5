
public class PhoneApplication {

    public static void main(String[] args) {

        new PhoneFrame();

       /* For the end message it would be something like:

        if (okayPixels >= 2048) {
            Message.ResultMessage("The product is working okay!");
        } else {
            Message.ResultMessage("The product is defective!");
        }

        Where "okayPixels" will be a variable which contains the number of the working pixels.
        The total number of pixels is (64x64) 4096,
        if there are less than 2048 working pixels the product is considered defective */

    }
}
